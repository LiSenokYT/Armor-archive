// Убираем всё связанное с поиском и добавляем:

function showComingSoon() {
    alert('🔧 Раздел в разработке\n\nСистема входа и личные кабинеты появятся в будущих обновлениях!');
}

// Обновляем навигацию
document.querySelectorAll('.category-card').forEach(card => {
    card.addEventListener('click', (e) => {
        if (!e.target.classList.contains('explore-btn')) {
            const categoryName = card.querySelector('h3').textContent;
            alert(`🎯 Переход к категории: ${categoryName}\n\nНа отдельной странице будут:\n• Подробные фильтры по характеристикам\n• Списки всей техники категории\n• Сравнительные таблицы\n• Исторические периоды`);
        }
    });
});
